package aminecraftplugin.aminecraftplugin.utils;

public interface Function {

    public double f(double x);

}


    /*private static double weightFac = 1000;

    public static double function1(double stock, double x) {
        return (weightFac * stock) / (x + stock);
    }

    public static double function2(double weightFac, double stock, double x) {
        return 2 * weightFac * ((weightFac * stock) / (x + stock));
    }*/